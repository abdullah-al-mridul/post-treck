import SettingsClient from "@/components/SettingsClient";

export const metadata = {
  title: "Settings | Post Treck",
  description: "Customize your Post Treck experience",
};

export default function Settings() {
  return <SettingsClient />;
}
